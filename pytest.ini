[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: DB-backed flows; Postgres via INTEGRATION_DATABASE_URL else SQLite fallback (no skips).
    unit: Pure schema/helper tests (fast; no FastAPI TestClient).
